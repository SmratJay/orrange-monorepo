# CRITICAL SECURITY: Environment Variables Configuration

# ⚠️  NEVER COMMIT ACTUAL SECRETS TO VERSION CONTROL ⚠️ 
# This file shows required environment variables for production deployment

# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/orrange_p2p"
REDIS_URL="redis://localhost:6379"

# JWT Secrets - MUST BE CRYPTOGRAPHICALLY SECURE RANDOM VALUES
# Generate using: openssl rand -base64 64
JWT_ACCESS_SECRET="your-ultra-secure-access-token-secret-here"
JWT_REFRESH_SECRET="your-ultra-secure-refresh-token-secret-here"

# Blockchain Configuration (Production: Use Vault/AWS Secrets Manager)
# ⚠️  CRITICAL: Never store private keys in environment variables in production
# Use HashiCorp Vault, AWS Secrets Manager, or similar secret management service
RPC_URL="https://mainnet.infura.io/v3/your-project-id"
ETHEREUM_PRIVATE_KEY="0x..." # 🚨 PRODUCTION: MUST USE SECRET MANAGEMENT SERVICE

# Monitoring & Observability
SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# Email & Notifications (Production values needed)
SMTP_HOST="smtp.your-provider.com"
SMTP_PORT="587"
SMTP_USER="your-email@domain.com"
SMTP_PASS="your-email-password"

# Rate Limiting & Security
RATE_LIMIT_MAX_REQUESTS="100"
RATE_LIMIT_WINDOW_MS="60000"

# File Upload & Storage
MAX_FILE_SIZE="10485760" # 10MB in bytes
ALLOWED_FILE_TYPES="image/jpeg,image/png,application/pdf"

# Production Environment Settings
NODE_ENV="production"
PORT="3001"
LOG_LEVEL="info"

# CRITICAL PRODUCTION DEPLOYMENT NOTES:
# 1. Use a dedicated secret management service (Vault/AWS Secrets Manager)
# 2. Rotate secrets regularly (every 90 days minimum)
# 3. Use different secrets for staging vs production
# 4. Enable secret auditing and access logging
# 5. Never log or expose secrets in application code
